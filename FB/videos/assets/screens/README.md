# Screen recordings & stills

Drop capture files here per `../shot-lists/screen-capture-checklist.md`.

Prefer short clips (`a1-dashboard.mp4`) plus a few PNGs for covers.
Large multi-minute dumps → `../raw/` instead.
