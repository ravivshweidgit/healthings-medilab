# prompt95 — Data-viz + chat + content/web/RTL (UI/UX audit Batches D+E)

**Status:** Partial — Batch D (D1+D2+D3) phone-approved (2026-07-24); Batch E pending.
Derived from `prompt92.txt` review output (Opus 4.8). Covers Batches D+E after
prompt93 (A+B) and prompt94 (C). Presentation/UX only — **no** changes to stored data,
units, clinical numbers, AsyncStorage keys, or rule interpretation.

**Builds on:** prompt94 (icon registry `theme/icons.tsx`, primary-tier accent), prompt93,
prompt92 (audit). Honors language-policy, persistence-parity, ai-judgment rules.

## Findings in scope

| ID | Sev | Where | Problem → fix |
|----|-----|-------|---------------|
| **F3** | P0 | Trend & energy chart | 4 differently-scaled series on one plot; left-axis numbers overlap/unreadable. → Per-series mini panels (reuse ENERGY sparkline stack) **or** index to % change with one axis. Values unchanged. |
| **F8** | P1 | Chat input toolbar | Too many cryptic buttons (Camera, ↑, ↓, Clear, ↗, Send). → **Shipped (compromise):** Camera · ↑ · ↓ · ⋯ · Send; Clear + Export in overflow menu (scroll arrows kept per phone test). |
| **F9** | P1 | Dashboard sync | "Refresh my data" only at very bottom. → **Shipped:** compact refresh icon in the top brand header (same sync); pull-to-refresh retained. |
| **F13** | P2 | Rules / Targets / session summaries | Detected-RTL (Hebrew/Arabic) user content shows left-aligned in EN chrome. → **Shipped:** shared `contentAlignStyle` (`logic/textDirection.ts`) aligns by detected script in My Rules, nutrition sessions, and macro reasoning. |
| **F15** | P2 | Edit Meal | Two delete concepts: per-item "Delete" + footer pink trash. → Clarify footer = delete whole meal (label/confirm copy). |
| **F19** | P2 | Website | Sparse desktop help (narrow column, big margins); landing H1 "A full metabolic OS" is jargon. → Widen/format help; patient-value H1. |
| **F20** | P2 | CGM blood-drop illustration | Reads alarming/heavy. → Soften (lighter fill / smaller drop / friendlier mark). |

## Hard constraints (do not fight)

- **No clinical-number or data changes.** F3 re-presents the *same* values — no recompute,
  no unit change. Keep the documented chart color map (weight/fat/muscle/visceral/
  glucose/HR) consistent everywhere.
- **RTL alignment by detected script only** (F13) — presentation. Do **not** parse rule/
  directive *meaning*; detect script (existing Hebrew/Arabic detection) and set
  `textAlign`/`writingDirection`. No new rules-text regex (ai-judgment rule).
- **No new AsyncStorage keys / no changed collapse or refresh semantics** (persistence-parity).
- Icons come from `theme/icons.tsx` (add to registry, don't scatter Lucide imports).
- Chat message **bodies/exports keep emoji**; only toolbar chrome changes (F8).
- Website: help path locale stays `/{lang}/help/…` (language-policy); clinic portal untouched.

## Shipped slices

| Slice | Finding | What | Commit |
|-------|---------|------|--------|
| **D1** | F3 | Trend chart titled mini-panels (WEIGHT / FAT·MUSCLE / VISCERAL) — axis labels no longer collide | `8c8e119` |
| **D2** | F8 | Chat toolbar: Camera · ↑ · ↓ · ⋯ · Send; Clear + Export in overflow (`ActionIcons.overflow/share/clear`) | `4cf29dd` |
| **D3** | F9 | Compact refresh icon in the top brand header (`ActionIcons.refresh`) → same sync as bottom button; spinner while busy | (this commit) |

## Shipped slices (Batch E)

| Slice | Finding | What | Commit |
|-------|---------|------|--------|
| **E1** | F13 | `logic/textDirection.ts` (`isRtlText` / `contentAlignStyle`) → detected-script align in My Rules, nutrition sessions, macro reasoning | (this commit) |

## Remaining slices (each own `bi`/site check + commit)
5. **Slice E2 · Edit Meal delete clarity (F15)** — footer copy/confirm = whole meal.
6. **Slice E3 · CGM illustration (F20)** — soften the drop mark.
7. **Slice E4 · Website (F19)** — help layout width/formatting + landing H1 copy.

Stop after each slice for confirmation (app slices need phone `bi`; E4 is web).

## Primary files (to confirm during impl)

| Concern | Likely file(s) |
|---------|----------------|
| Trend chart (F3) | `app/src/components/MetabolicChart.tsx` + trend section in `DashboardScreen.tsx`; ENERGY sparkline stack as pattern |
| Chat toolbar (F8) | `app/src/screens/ChatScreen.tsx` (`inputToolbar`, `attachBtn`, `scrollBtn`, clear/expand) |
| Refresh (F9) | `app/src/screens/DashboardScreen.tsx` (`RefreshControl handlePullRefresh`, "Refresh my data" action) |
| RTL user content (F13) | `RulesStrip.tsx`, `NutritionDirectivesStrip.tsx`, targets display, shared align helper |
| Edit Meal delete (F15) | `app/src/components/FoodLogModal.tsx` |
| CGM illustration (F20) | `app/src/components/GearIllustrations.tsx` / CGM mark component |
| Website (F19) | `website/` help pages + landing |

## Do NOT touch (content, not chrome)
`GeminiService.ts`, `mentorChatText.ts`, `mentorChatExport.ts`, `chatIntent.ts`,
`ReviewService.ts`, `mealGlucoseAnalysis.ts`, clinic portal. Chart values/formulas.

## Phone/site-tested checklist
- [x] Trend chart legible on small screen; no overlapping axis labels; same values (D1)
- [x] Chat input: Camera · ↑ · ↓ · ⋯ · Send; Clear/Export in overflow (D2, scroll kept)
- [x] Refresh reachable without scrolling to bottom; existing bottom action still works (D3)
- [x] Hebrew/Arabic rules/targets/session text right-aligns in EN chrome; LTR unaffected (E1)
- [ ] Edit Meal: clear that footer trash deletes the whole meal
- [ ] CGM illustration softer, not alarming
- [ ] Website help readable full-width on desktop; landing H1 patient-friendly
- [ ] No changed AsyncStorage keys; refresh/collapse semantics intact

## Related
- `prompt92.txt` — audit source (F3, F8, F9, F13, F15, F19, F20).
- `prompt93.txt`, `prompt94.txt` (done) — batches A–C.
