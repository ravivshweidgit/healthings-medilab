# prompt113 — Lab country gate + country-scoped lab PDF models

**Status:** in_progress (implemented 2026-08-16 — needs VPS migrate + phone test)  
**Builds on:** prompt112 (IL HMO identify → confirm → layout pack)  
**Backend:** [`../../backend/be-43-lab-catalog.md`](../../backend/be-43-lab-catalog.md)

## Owner UX (locked)

- Country picker under **Lab Reports** only (not Profile / QS).
- **No PDF import** until country is selected (no silent `IL` default).
- Provider / country **display names** from server: `name_native ?? name_en`  
  (country brand language — **not** `appLocale`). Canonical `name_en` always stored.

## What shipped (code)

| Piece | Change |
|-------|--------|
| DB | `lab_countries`, `lab_providers`, `lab_prompt_packs` + IL/US seed |
| API | `GET /v1/lab/countries`, `GET /v1/lab/catalog/:countryCode` |
| `lab_country` AsyncStorage | null until user picks; backup/export auto-includes |
| Lab strip + modal | Country gate; Change country; packs into identify/parse |
| GeminiService | Accepts server packs; embedded IL fallback if API offline |

## Phone checklist

- [ ] Fresh install: cannot import until country chosen
- [ ] IL → Meuhedet: chips show מאוחדת (native), parse still good
- [ ] App language English + lab country IL → still native HMO names
- [ ] US: no IL HMO chips; generic parse
- [ ] After VPS migrate: catalog from API (not only fallback)

## Related

- prompt112 — IL two-pass (done)
- be-43 — server catalog
- language-policy — country ≠ appLocale; lab test `name` stays English at rest
