# prompt107 — In-app explainer videos (Watch how-to)

**Status:** in progress (hosting live 2026-08-08; app Watch wiring local — phone-test / commit next)  
**Live:** `https://healthings.ai/{en|de|fr}/watch/{id}.html` + `videos/{en|de|fr}/{id}.mp4`  

**Test order:** Samsung Android first (`bi`) → iOS later (`bi-os`)  
**Builds on:** prompt106 (`WhatsNextCard`, Help strip), `WelcomeQuickStartWizard`, `helpUrls` / help site (prompt81+), FB films under `FB/videos/` (clips 01–09)  
**Positioning:** Improves **setup ease** + **UX / polish / retention** by teaching habits inside the app (Withings-style tips), without shipping mp4s in the APK.

## Problem

Explainer films already exist for FB / website (meal entry, grams, Activity/YouTube, gear, closed loop, clinic→rules→coach). Users who install the app never see them unless they find the site. What’s next tells them **what to do** (Log meal / Add activity) but not **how it looks** in 20–40s.

Quick Start also confuses some users at **“Do you have a Withings scale?”** — they hear “app requires Withings,” not a routing question. That is primarily a **copy** problem (No-first lead); video is optional backup, not a new required step.

## Goal

Surface a small catalog of **hosted** explainer videos inside the app — optional “Watch” paths next to existing Help / What’s next — so first-session and habit learning match the marketed story.

## Non-goals

- Do **not** embed large video files in the APK / IPA
- Do **not** replace What’s next primary CTAs with video (actions stay first)
- Do **not** build a full in-app Netflix / course system
- Do **not** require new Hebrew VO — ship EN VO (**Matilda**) + HE burned subs + Max Oazo instrumental bed, steady picture (see `.cursor/rules/explainer-video-pipeline.mdc`) until a later locale pass
- Do **not** block prompt106 B/C micro-polish; this can ship after or in parallel as its own batch

---

## Product rules

### Placement (pick in implementation; prefer A+C)

| Surface | Role |
|---------|------|
| **A. What’s next** | Secondary text link only: e.g. “Watch: log a meal” — never a third primary pill competing with Log meal / Add activity |
| **B. Food Log / Activity Log** | Optional `?` or “How to” near Add — deep-link to the matching clip |
| **C. Help strip** | Section **Watch explainers** — list of 3–6 titles → open video URL (same pattern as help articles) |
| **D. Your setup / Gear** | Optional one “Watch: connect gear” after skip/link — only if it doesn’t add setup steps |
| **E. Quick Start gear Yes/No** | Optional secondary **Watch** under scale / watch / CGM leads → matching clip. Never autoplay; never a required step before Yes/No |
| **F. Quick Start phone-health** | After **Watch = No** (and/or CGM Yes): secondary Watch → `phone-health` / `cgm-pipeline` — these are the high-confusion permission films |

**Default v1 (host existing):** Help strip list (C) + What’s next meal Watch (A) + optional `gear`.  
**Default v1.1 (produce next):** `phone-health` + `cgm-pipeline` — owner-confirmed need; storyboards in `FB/videos/storyboards/10-*.md` / `11-*.md`.

### Quick Start — locked decisions (2026-08-08)

| Decision | Detail |
|----------|--------|
| Soften copy first | Scale / watch / CGM leads lead with **No is fine** → then Yes path. Shipped in `quickStartCopy.ts` (all 10 locales) ahead of video hosting. |
| No forced video in wizard | Opt-in Watch only; do not insert a film step into `buildStepList`. |
| Reuse `gear` (clip 03) for brand overview | Do **not** produce a “why Withings?” film until copy + tip still fail. Clip 03 does **not** replace permission films. |
| **New films required** | `phone-health` (Watch No → HC / Apple Health write→read) and `cgm-pipeline` (CareSens → xDrip live vs end-of-day writers). See catalog v1.1. |
| Help `?` stays | Existing help articles + in-app Withings tip remain; Watch is additive. |

### Hosting

| Rule | Detail |
|------|--------|
| Origin | `https://healthings.ai/…` (or CDN under same brand) — gitignored local exports stay in `FB/videos/assets/exports/` |
| Format | Vertical 9:16 mp4 already rendered (`*-en-subhe-9x16.mp4`) |
| Delivery | Direct HTTPS mp4 **or** a thin HTML watch page that plays the file (easier analytics / poster / back link to help) |
| App open | `Linking.openURL` (browser / system player) **or** `expo-av` / WebView only if offline/poster needs justify the dependency — prefer URL open for v1 |
| APK | Zero video bytes |

### Catalog (v1 — host existing habit / gear clips)

| Id | Clip (FB) | Title intent (EN; localize like a person) | When to show / link |
|----|-----------|---------------------------------------------|---------------------|
| `meal-entry` | 09 | How to log a meal (photo, text, past) | What’s next + Help + optional Food Log |
| `meal-grams` | 08 | Adjust grams / items | Help (+ Food Log after first meal optional) |
| `activity-youtube` | 07 | Add activity from YouTube | What’s next (if Activity on) + Help + Activity Log |
| `gear` | 03 | Connect scale / watch / CGM (overview) | Help + optional Gear / Quick Start |
| `scale-choice` | 13 | Withings scale Yes/No (No is fine) | Quick Start scale Watch |
| `scale-trends` | 12 | Scale → body composition → Trend & Energy → BMR | Help payoff (not QS Yes/No) |
| `closed-loop` | 05 or 06 | What Healthings is (loop) | Help only — not What’s next |

### Catalog (v1.1 — **produce**; setup ease / permission literacy)

These are **not** covered by clip 03. Users who tap Watch = No or CGM = Yes hit a technical model: other apps **write** to Health Connect / Apple Health; Healthings needs **read** permission.

| Id | Storyboard | Title intent | Teaching must-land | When to show / link |
|----|------------|--------------|--------------------|---------------------|
| `phone-health` | [`10-phone-health.md`](../../../FB/videos/storyboards/10-phone-health.md) | Phone health: who writes, who reads | Watch No → wearable apps write steps/HR into HC / Apple Health; Healthings Allow = **read**; do not confuse with Withings cloud | Quick Start **watch Yes/No** + phone-health step; Help `phone-health-activity` |
| `cgm-pipeline` | [`11-cgm-pipeline.md`](../../../FB/videos/storyboards/11-cgm-pipeline.md) | Live CGM vs end-of-day | Android CareSens **live** needs **xDrip+ → Health Connect**; Samsung Health (etc.) often **end of day** only; iOS = share Blood Glucose → Apple Health; CSV Import ≠ live | Quick Start CGM / phone-health; Help `cgm`; CareSens chip empty/allow states |
| `scale-choice` | [`13-scale-choice.md`](../../../FB/videos/storyboards/13-scale-choice.md) | Withings scale — Yes or No | No is fine → enter weight; Yes → cloud composition (not Bluetooth) | Quick Start scale |
| `scale-trends` | [`12-scale-trends.md`](../../../FB/videos/storyboards/12-scale-trends.md) | Scale, composition, energy, BMR | Body strip → Trend & Energy → BMR floor | Help only |

Defer reel series 01/02/04 (clinic/rules/coach) unless Help “Learn more” wants the full story.

### Copy / language

- UI chrome: `appLocale` catalogs (`*Copy.ts`); speak-like-a-person; no Latin mid-RTL in running prose
- Always-English glossary on video stays (kcal, CGM, Withings…) — language-policy
- Titles/descriptions in all 10 `SUPPORTED_LANGUAGES`

### Locale / VO ladder (locked 2026-08-08 — clarity + ship speed)

| Priority | Ship | Rationale |
|----------|------|-----------|
| **v1** | EN Matilda VO + HE burned subs on every clip | FB / IL social + master; picture once |
| **v1.1 VO** | **FR + DE spoken** Matilda on **`phone-health` + `cgm-pipeline` only** | Permission literacy fails if users only skim EN while reading subs |
| **Deferred** | FR/DE VO on meal/activity; any VO for es/pt/it/ar/ru/tr | Until a market pull; catalog titles still localize |
| **Never (default)** | Hebrew **spoken** VO | Prior quality fail — HE stays burned on EN |
| **In-app** | Open `appLocale` watch URL when file exists; else EN master | Don’t block Quick Start on missing FR/DE |

Subtitle look: **outline only** (no 0.3 full-width band). Mix rule: `.cursor/rules/explainer-video-pipeline.mdc`.

### Persistence

- Optional: `healthings:explainerWatched_v1` map of clip id → watched — for “New” badges or hiding a tip; **not** required for v1
- Do **not** tie to `whatsNextDismissed_v1` — dismissing What’s next must not hide Help videos

---

## Implementation sketch

1. **Host** — Copy or symlink chosen exports to website deploy path (e.g. `website/assets/videos/` or `website/en/watch/…` HTML wrappers). Update deploy script if needed. No secrets.
2. **URL helper** — e.g. `explainerVideoUrl(id)` next to `helpUrl` (stable ids, not FB export folder numbers).
3. **Catalog module** — id, title key, duration hint, url, optional `helpsWith: 'meal' | 'activity' | 'gear' | 'story'`.
4. **UI** — Help strip section; What’s next secondary link → `meal-entry` (and activity clip if strip visible).
5. **i18n** — new `explainerCopy.ts` (or extend `helpStripCopy` / `whatsNextCopy`).
6. **Phone test** — open each URL on Android; confirm portrait play; dark/light chrome around the link looks intentional.

### Primary files (expected)

| Area | Paths |
|------|--------|
| App | `app/src/i18n/explainerCopy.ts` (or help), `helpUrls.ts` or `explainerUrls.ts`, `HelpStrip.tsx`, `WhatsNextCard.tsx`, `DashboardScreen.tsx` |
| Site | `website/…` watch pages and/or `assets/videos/*.mp4` + deploy |
| Specs | `FB/videos/NEXT.md` note “also in-app via prompt107” |

---

## Acceptance

- [ ] At least **3** explainers reachable from Help without leaving “help” mental model (meal entry, activity, closed-loop or gear)
- [ ] What’s next still shows Log meal / Add activity as primary; Watch is secondary or absent if dismissed
- [ ] No mp4 in app binary / git LFS surprise — URLs only
- [ ] 10-locale titles for list rows; video may stay EN+HE-subs
- [ ] Samsung `bi`: tap → video plays; back to app; no crash if offline (calm error / browser message)
- [ ] iOS later: same URLs
- [x] prompt106 polish score narrative — `COMPETITOR-SCORES.md` 2026-08-08: Setup **56→64**, UX/polish **60→63** / **64→67**; overall solo **82**, +clinic **90**

## Shipped ahead of video hosting

- [x] Quick Start scale / watch / CGM copy — No-first leads (2026-08-08) so “No Withings” does not read as blocked

## Produce next (owner priority — 2026-08-08)

1. [x] Script + render **`phone-health`** + **`cgm-pipeline`** EN masters (Matilda + HE burns + instrumental + steady)  
2. [x] Host + wire Help / Quick Start Watch (EN; `appLocale` fallback)  
3. [x] Translate + render **FR + DE VO** for those two permission films only  
4. [x] **`scale-trends`** (012) — Help payoff; EN+HE + DE  
5. [x] **`scale-choice`** (013) — QS scale Yes/No Watch; EN+HE + DE  
6. Habit-clip FR/DE VO and other locales — only after watch analytics / clinic ask  
7. [x] Live Trend & Energy still (`a2-trend-energy.jpg`) in clip 12  
8. Longer “why Withings?” brand film — only if scale-choice + tip still fail

## Deferred

- In-app fullscreen player with posters
- Per-locale dubbed VO
- Auto-play / forced tutorial on first launch (avoid; opt-in Watch only)
- Clinic portal embedding same assets
- New film “why we ask about Withings” — only if copy + tip still fail on phone

## Related

- `prompt106.txt` — What’s next + polish; this is education layer, not habit-speed UI
- `prompt76` — Watch off → phone health (HC / Apple Health) product rules
- `prompt34` / `prompt21` — CareSens → xDrip+ → Health Connect live path
- `FB/videos/NEXT.md` / `PLAN.md` — source films + produce queue
- `helpUrls.ts` — same origin / locale patterns where possible
- Competitor: Nutrisense / Withings in-product education vs Healthings help-site-only today
