# Explainer videos (prompt107)

Stable ids under `en/`, `de/`, `fr/`. Generated by `python FB/videos/production/publish_explainers.py`.

mp4s are gitignored — run publish on the machine that deploys the site, then `bash server/scripts/deploy-website.sh`.

Watch pages: `/{en|de|fr}/watch/{id}.html`
