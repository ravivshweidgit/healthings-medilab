# Secret leak prevention — run once: pip install pre-commit && pre-commit install
# Manual scan: pre-commit run --all-files

repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.4
    hooks:
      - id: gitleaks
